[monitoring]
frequency = 1
disk_paths = ["/"]

[labels]
environment = "production"
service = "beacon-daemon"

[logging]
level = "debug"

[telemetry]
server = "http://localhost:3000"
retry_interval = 5
interval = 10
